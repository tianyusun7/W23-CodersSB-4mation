import React from 'react';
import { FlatList } from 'react-native';
import JoinedBoardSidePanel from './JoinedBoardSidePanel';

const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      alignItems: 'flex-start',
      justifyContent: 'left',
      padding: 10,
    },
    text: {
      fontSize: 16,
      fontWeight: 'bold',
    },
    button: {
        
    }
  });

