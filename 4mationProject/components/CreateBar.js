// this component contains a button that directs the user to CreateBoard/Screen/Comment screens
// depending on which screen the component is embedded in. 
// so a prop might be used to determine which screen this button is in. 